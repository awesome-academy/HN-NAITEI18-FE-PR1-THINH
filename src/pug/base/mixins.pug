mixin linkItemNav(href, title, isDropDown)
  if isDropDown
    li.nav__link
      .dropdowm
        a.dropdown-toggle(class!=attributes.class, href=href, data-bs-toggle="dropdown")=title
        div.dropdown-menu.nav-dropdown__menu
          .nav-dropdown__wrapper
            +linkGroup('Dưỡng da', navLinks1, false)
            +linkGroup('Nước hoa', navLinks2, false)
            +linkGroup('Trang sức', navLinks2, false)
  else
    li.nav__link
      a(class!=attributes.class, href=href)=title
      i.fa-solid.fa-chevron-down

mixin linkGroup(title, linkTitles, isFooter)
  if isFooter
    -var groupClass = footerGroupClass
    -var linksClass = footerLinksClass
    -var itemClass = footerItemClass
    -var linkClass = footerLinkClass
    -var titleClass = footerTitleClass
  .link-group-cus(class=groupClass)
    h5.link-group__title(class=titleClass)= title
    ul.link-group__links(class=linksClass)
      each item in linkTitles
        li.link-item__wrapper(class=itemClass)
          a.link-item(class=linkClass, href="#")= item

mixin promotionTag(iconClass, title)
  li(class!=attributes.class)
    i(class=iconClass)
    p= title 

mixin logo()
  .logo__first-charbox  
    p.logo__first-char m
  h3.logo__char andala
  

mixin productCard(imageUrl, alt, productTag, productName, productPrice, productOldPrice, withButton)
  div(class!=attributes.class)
    img.product__image(src=imageUrl, alt=alt)
    h5.product__tag= productTag 
    h3.product__name= productName
    img.product__line
    p.product__price= productPrice
      span.product_sub-price= productOldPrice
    if withButton 
      +productBtn()

mixin productBtn()
  .product__btn-box 
    button.btn.btn-outline-dark.btn__add MUA HÀNG
    button.btn.btn__like
      i.fa-solid.fa-heart
    button.btn.btn-outline-dark.btn__load
      i.fa-solid.fa-rotate

mixin contact(iconClass, title)
  div(class!=attributes.class)
    i(class=iconClass)
    p= title 

mixin cartDropdownItem(imageUrl, title, price)
  .cart-dropdown
    img(src=imageUrl, alt=title)
    .item__info
      p.item__title= title
      p.item__price= price

mixin categoryItem(title)
  if block
    .category-item__wrapper.category-item--open
      p.category-item__title= title
      i.fa-solid.fa-caret-down
    ul.category-item__line
      block
  else
    .category-item__wrapper
      p.category-item__title= title
      i.fa-solid.fa-caret-right

mixin pagation()
  .pagation-box
    .pagation--left
      i.fa-solid.fa-border-all
      i.fa-solid.fa-list
    .pagation--right
      i.fa-solid.fa-caret-left
      p.pagation__page.active 1
      p.pagation__page 2
      p.pagation__page 3
      i.fa-solid.fa-caret-right

mixin productItemList(imageUrl, alt, productTitle, productDes, productPrice)
  div(class!=attributes.class)
    img.proditem-list__image(src=imageUrl, alt=alt)
    .proditem-list__content-box
      h4.proditem-list__title= productTitle
      .proditem-list__sold
        .sold__heart-icon
          i.fa-solid.fa-heart
          i.fa-solid.fa-heart
          i.fa-solid.fa-heart
          i.fa-solid.fa-heart
          i.fa-solid.fa-heart
        p.sold_quantity= '( 4 lượt mua )'
      .proditem-list__des= productDes
      .proditem-list__price= productPrice
      .proditem-list__btn-box
        +productBtn()
